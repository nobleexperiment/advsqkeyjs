extends ../layouts/default

block content
	.container: .jumbotron
		img(src='/images/logo.svg', width='160')
		h1 Imagining AdvisorSquare
		p In my free time, i'm entertained with the idea of "what ifs?". What if in my past startup AdvisorSquare was upgraded from a legacy ASP app to a javascript app.
		p AdvisorSquare was a customized CRM serving the Financial Services Industry with 
		if user && user.canAccessKeystone
			p
				a(href='/keystone').btn.btn-lg.btn-primary Open the Admin UI
		else
			p We have created a default Admin user for you with the email <strong>admin@admin.admin</strong> and the password <strong>admin</strong>.
			p
				a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
				|  to use the Admin UI.

